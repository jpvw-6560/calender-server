const express = require('express');
const router = express.Router();
const calendarController = require('../controllers/calendarController');


// (Routes legacy supprimées, API REST uniquement)

module.exports = router;
